package org.campus02.palindrom;

public class PalindromChecker {

    /**
     * In this example of recursion,
     * we first check to see if the first and last letters of a String are the same.
     * We then move one letter in from both the start and the end of the String
     * and recursively perform the same String comparison.
     * If a letter is not a letter (not alphabetic), remove the letter and take the next one
     * If all the checks return true, then our Java palindrome program returns true.
     * If not, the palindrome checking program returns false.
     *
     * @param word the text
     * @return true in case of palindrome found
     */
    public boolean check(String word) {
        return true;
    }
}
